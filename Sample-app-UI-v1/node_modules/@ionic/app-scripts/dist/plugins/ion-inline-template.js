"use strict";
var template_1 = require('../template');
var pluginutils = require('rollup-pluginutils');
function ionInlineTemplate(options) {
    options = options || {};
    var filter = pluginutils.createFilter(options.include || ['*.ts+(|x)', '**/*.ts+(|x)'], options.exclude || ['*.d.ts', '**/*.d.ts']);
    return {
        name: 'ion-inline-template',
        transform: function (sourceText, sourcePath) {
            if (filter(sourcePath)) {
                return template_1.inlineTemplate(sourceText, sourcePath);
            }
        }
    };
}
Object.defineProperty(exports, "__esModule", { value: true });
exports.default = ionInlineTemplate;
